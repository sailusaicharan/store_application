import 'package:flutter/material.dart';

class NutsPage extends StatelessWidget {
  const NutsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Center(child:  Text('')),
      ),
      body: const Center(
        child: Text('Nuts page'),
      ),
    );
  }
}
